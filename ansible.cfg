[defaults]
INVENTORY = hosts
# Service playbooks live in docker/, so Ansible's playbook-adjacent lookup only
# finds docker/roles. Add the repo-root roles/ dir so shared roles (e.g.
# arr_apikeys) resolve regardless of which playbook references them.
ROLES_PATH = roles
