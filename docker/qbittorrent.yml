---
- name: Qbittorrent
  vars:
    service: "qbittorrent"
    service_port: "4343"
    service_labels:
      - key: "autoheal-app"
        value: "true"
      - key: "wud.tag.include"
        value: '^v?\d+\.\d+\.\d+(\.\d+)?(-r\d+)?-ls\d+$'

  hosts: all
  vars_files:
    - ../vars/vault.yml
    - ../vars/traefik_labels.yml

  tasks:
    - name: Start qbittorrent
      community.docker.docker_container:
        image: linuxserver/qbittorrent:5.1.4-r2-ls448
        name: "{{ service }}"
        env:
          PUID: "1000"
          PGID: "1000"
          TZ: Europe/Amsterdam
          WEBUI_PORT: "4343"
        volumes:
          - "{{ docker_config_dir }}/{{ service }}:/config"
          - "{{ docker_nas_dir }}/Download:/downloads"
        restart_policy: unless-stopped
        network_mode: container:vpn
        labels: "{{ (traefik_default_labels + service_labels) | items2dict }}"
        state: healthy
        healthcheck:
          test: bash -c 'echo > /dev/tcp/localhost/4343'
          interval: 30s
          timeout: 10s
          start_period: 60s
