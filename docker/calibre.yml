---
- name: Calibre
  vars:
    service: "calibre"
    service_port: "8080"
    service_labels:
      - key: "autoheal-app"
        value: "true"

  hosts: all
  vars_files:
    - ../vars/vault.yml
    - ../vars/traefik_labels.yml

  tasks:
    - name: Start calibre
      community.docker.docker_container:
        image: lscr.io/linuxserver/calibre:latest
        name: "{{ service }}"
        env:
          PUID: "1000"
          PGID: "1000"
          TZ: Europe/Amsterdam
          CUSTOM_HTTPS_PORT: "8213"
        volumes:
          - "{{ docker_config_dir }}/{{ service }}:/config"
          - "{{ docker_nas_dir }}:/books"
        restart_policy: unless-stopped
        network_mode: container:vpn
        labels: "{{ (traefik_default_labels + service_labels) | items2dict }}"
        healthcheck:
          test: bash -c 'echo > /dev/tcp/localhost/8080 && echo > /dev/tcp/1.1.1.1/443'
          interval: 30s
          timeout: 15s
          start_period: 60s
